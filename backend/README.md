# LegalAdvisor Backend

Simple Express + MongoDB backend scaffold for the LegalAdvisor project.

Setup

1. Install dependencies:

```bash
cd backend
npm install
```

2. Configure environment variables in `.env` (see `.env` placeholder).

3. Run in development:

```bash
npm run dev
```

API

This scaffold includes basic user auth (JWT), user profile routes and simple post CRUD.
